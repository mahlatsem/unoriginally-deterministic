---
topic: unoriginally-deterministic PRD
updated: 2026-07-19T19:47
---

- (decision) Working mode: Fast path (user deferred to my suggestion). Reason: PRFAQ distillate already locks architecture, v1 scope, and competitive positioning — coaching would re-derive decisions already made. Stakes: ship broadly.
- (change) Corrected source PRFAQ + distillate: reliability go/no-go experiment scoped to Claude Code only (not 3 different AI assistants) — user builds exclusively with Claude Code; cross-assistant portability is out of scope, left for other users to validate on their own.
- (change) Corrected source PRFAQ + distillate: tool code can be hand-written, AI (Claude Code) just makes it faster — not the exclusive authoring path. Softened 'AI-authored'/'AI writing the tool's own code' framing in architecture-decision and posture sections of both docs.
- (decision) Design principle (locked): the extension contract must be easy to build against for BOTH AI and a human writing a tool by hand — AI use is recommended, especially for speed, but never assumed as the only path. Propagated into reliability-mechanism and provenance-design passages of PRFAQ + distillate. This becomes a PRD requirement/NFR: extension contract usability is not AI-exclusive.
- (decision) Success = personal dogfooding outcome only, showcased publicly; explicitly NOT measuring other users' adoption/activity (no stars/installs targets, no counter-metric on external usage).
- (decision) First-language target for v1 starter view set: COBOL first (not TS/JS) — most pressing personal comprehension need, aligned with legacy-modernization beachhead framing.
- (decision) Rollout: phased (dogfood -> invited beta -> public listing). NFRs left open/deferred to architecture. Community contributions closed at v1 launch, opened later.
- (event) Full PRD draft written (Fast path) from PRFAQ distillate + batched-gap answers. 4 assumptions tagged, 5 open questions, sections: Vision, Target User, Glossary, Features (FR-1..FR-5), Non-Goals, MVP Scope, Success Metrics, Open Questions, Assumptions Index.
- (idea) Candidate answer to OQ-5 (striking-enough showcase): self-documentation - run a molded tool against the toolkit's own source (e.g. dependency map of the extension-contract code), showcased directly in VS Code. Inspired by GT's own book (book.gtoolkit.com), built live in GT's Lepiter notebooks as proof-by-use of moldable development, but without GT's cost of entry since it stays inside VS Code. Not committed as final, but leading candidate. Folded into PRD OQ-5 and SM-3.
- (change) Input reconciliation vs source PRFAQ found 4 gaps, all fixed in prd.md: (1) phased rollout (dogfood->invited beta->public VS Code Marketplace listing) was decided but never written into the PRD - added as new 6.2 Rollout Plan; (2) added explicit non-goal 'does not catch semantic/local correctness bugs' (accepted product ceiling from PRFAQ Q6); (3) added 'no source code leaves the machine' as an explicit FR-3 consequence; (4) added zero-setup/no-account starter-view consequence to FR-4.
- (decision) Reviewer gate (rubric + adversarial + edge-case, all 3 selected by user) ran. Fixes applied: (1) added FR-2 - TypeScript extraction in v1, scoped ONLY to self-documenting the toolkit's own source (runtime/primitives/extension contract), distinct from COBOL user-facing extraction (FR-1) - resolves the OQ-5 self-documentation contradiction since it's now buildable in v1; user's reasoning: can't build the toolkit without TS, can't build COBOL tools without documenting the toolkit. (2) User chose option (a) on Java: v1 stays COBOL-only for user-facing comprehension; added explicit Non-Goal 'not cross-language COBOL<->Java tracing' - v1 solves the COBOL part of the real problem, not the full entanglement. (3) Fixed false claim that molding itself is local-only (it uses cloud Claude Code) - scoped 'no source leaves machine' claim to extraction/running only. (4) Added concrete SM-2 threshold (5 tasks, 4/5 pass, tagged ASSUMPTION). (5) Added ERROR-node-in-view-rendering consequence to FR-1. (6) Added OQ-6 for rollout graduation criteria / beta rollback plan. FRs renumbered FR-1..FR-6 to insert new FR-2 in reading order.
- (event) Party-mode architecture brainstorm (installed roster) resolved v1 delivery shape: thin VS Code extension + TS-library primitives, MCP deferred, LSP-based parked for v2. Full detail written to addendum.md, not prd.md, since these are technical-how/mechanism decisions, not PRD-level capability requirements.
- (decision) Open-item triage: SM-2 threshold (5 tasks, 4-of-5 pass) confirmed by maintainer, no longer an assumption. Full test protocol (task selection, independent/unedited definitions, pass criteria, known single-judge limitation) written to addendum.md rather than expanding the PRD. Remaining Open Questions (OQ-1 through OQ-6) and Assumptions all reviewed: none are phase-blockers for handing this PRD to architecture/epics - all legitimately deferred (architecture's own job, resolved during build, or pre-launch decisions with time to spare). Carrying forward as-is, owner = maintainer in all cases.
- (event) Polish complete: structural pass (both docs) applied - merged duplicate Non-Goals/Out-of-Scope lists, moved Glossary to end (sections renumbered 0-9, all cross-refs updated), condensed repeated no-bundled-AI and COBOL/Java rationale, front-loaded OQ-1 gating status in Vision, fixed the assumption-tagging convention claim in §0, reordered/relabeled addendum sections, folded the standalone open-item note into the Decision section. Prose pass (both docs) applied - 5 fixes each (grammar, ambiguous cross-refs, awkward collocations, article/pronoun agreement). PRD finalized: status set to final.
- (event) PRD finalized
- (change) UPDATE INTENT (2026-07-19), driven by now-final UX spines at planning-artifacts/ux-designs/ux-unoriginally-deterministic-2026-07-19/. Reconciled PRD against UX DESIGN.md + EXPERIENCE.md.
- (override) OQ-3 CLOSED (was deliberately left open, memlog 2026-07-18 triage said 'resolved during build'). UX spine commits six perspectives across three grains: system (dependency map, sequence, reach tracer), component (call-flow), unit (flowchart, code). Override accepted by maintainer. FR-5 expands from 'at least one working starter view' accordingly.
- (decision) Phasing recorded, three tiers. GATE SLICE (subset of Phase A; what OQ-1/SM-2 runs against): COBOL extraction INCLUDING copybook expansion, provenance-enforcing primitives, dependency map only, click-to-source, unparsed + unresolved rendering. Maintainer chose ONE view for the gate per the existing SM-2 protocol step 1, over the UX's two-perspective Phase A. PHASE A REMAINDER (post-gate, completes day-one product): unit flowchart, gallery. PHASE B (post-gate): sequence, reach tracer, call-flow, trail history, provenance rail, Code-perspective sync, molded-tool perspective registration. Rationale matches the addendum's existing MCP-deferral principle: don't pay a cost before proving the core bet.
- (decision) Copybook/include EXPANSION committed to v1 and placed PRE-GATE (Phase A). Reason: real COBOL is copybook-saturated, so a gate run against non-expanded extraction would not resemble the dogfooding target and a pass might not transfer. New FR-1 consequence + provenance rule: an element derived through inclusion carries BOTH anchors (inclusion site + definition line), and REPLACING substitutions are stated plainly, so click-through never lands on a line that doesn't say what the node said.
- (decision) THIRD HONESTY STATE added alongside provenance and unparsed regions: UNRESOLVED TARGET (dynamic dispatch — invocation whose target is not statically resolvable, e.g. COBOL CALL identifier). Must render as a distinct first-class state, never resolved by heuristic and never silently dropped, and never confusable with an unparsed region (the parse succeeded; only the target is unknown). Lands as a consequence on FR-1/FR-4.
- (decision) GATE-FAILURE BRANCH defined (was undefined — PRD said 'nothing proceeds until OQ-1 resolves' but never said what a FAILURE means). Answer: a failed gate indicts the EXTENSION CONTRACT, not the thesis. Response = redesign the contract using what the failed attempts showed, then re-run the experiment. Phase A remainder and Phase B stay blocked until a pass. Retries are BOUNDED so the honest kill-signal survives — bound tagged [ASSUMPTION] pending maintainer confirmation.
- (decision) Phase B deliverables get REAL FRs (were named only in a §5.2 table cell — rubric critical: epics would invent them). New §3.5 adds FR-7 view gallery (Phase A), FR-8 view-trail history (Phase B), FR-9 provenance audit rail (Phase B), FR-10 molded-tool perspective registration (Phase B, and explicitly a post-gate extension-contract expansion rather than something smuggled in via phasing).
- (change) Reviewer + reconciliation fixes applied: (a) accessibility-floor scoping added as a §4 non-goal with its re-open trigger beside §5.3 rollout; (b) determinism claim bounded to STRUCTURE not pixel layout, with pixel-stable layout kicked to architecture as an explicit fork; (c) FR-3's unenforceable 'short enough to be read end-to-end' corrected — the toolkit STATES a molded tool's actual size rather than promising a length it cannot enforce; (d) audit-the-lens given a real FR consequence; (e) unresolved-target reconciled with FR-4 (a dynamic call DOES carry a span — the CALL site — so 'no source, no node' holds); (f) honest state added for an unresolvable COPY member; (g) OQ-2 now lists the contract requirements the UX run produced + the SM-2 frozen-minimal-contract constraint; (h) Glossary updated with scope/perspective/grain/gate slice and 'starter view' redefined; (i) FR-1 honesty consequences reframed as COBOL INSTANCES of general categories, not COBOL features.
- (event) Polish complete. Structural pass: 9 edits — deduped the phasing story (told in 6 places, now front-loaded in §1 + owned by §5.2), cut §3.5's defensive 'named here because' clause and FR-3's self-narration, condensed the two oversized §4 non-goals (commitments and re-open trigger preserved verbatim), moved the language-agnostic framing from a trailing FR-1 aside into §3.1's Description, split §0's fused update/conventions paragraph, cut hollow §5.4 (pure pointer to §4), deduped Glossary. Prose pass: 10 fixes — US/UK spelling drift I introduced (behavioural/colour/linearised), stale 'starter view/starter tools/hardcoded views' terminology after the Glossary rename, two tense errors ('what the gate measured' -> 'measures'), a comma splice, a stale quotation of addendum text updated in the same run, 'rendered invisible' domain ambiguity, and three clarity fixes. 5,900 -> 5,242 words (-11%).
- (event) PRD finalized (v2, 2026-07-19). status: final, updated: 2026-07-19.
