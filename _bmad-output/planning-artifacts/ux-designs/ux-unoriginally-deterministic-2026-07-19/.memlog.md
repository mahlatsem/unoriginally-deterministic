---
topic: UX for unoriginally-deterministic — a VS Code extension toolkit that draws deterministic, code-derived comprehension views (dependency maps, call-flows, structure) with structurally-enforced provenance
updated: 2026-07-20T09:08
---

- (decision) Mode=Create (fresh). Sources inherited by reference: PRD (prd-unoriginally-deterministic-2026-07-18/prd.md) + PRFAQ + PRFAQ distillate. Working path=Fast (batch gaps, draft both spines with [ASSUMPTION] tags, skip creative tools).
- (decision) Foundation/UI system = VS Code extension. Views render in VS Code Webviews. Both spines INHERIT from VS Code's theming system: use VS Code theme CSS variables (--vscode-*) so the toolkit adopts the user's active color theme (light/dark/high-contrast) rather than imposing a brand palette. This is the honest, native, on-machine posture the product demands. DESIGN.md specifies only the minimal brand-layer delta.
- (decision) Load-bearing UX invariants derived from PRD/PRFAQ (spines win on conflict): (1) Provenance is a first-class interaction — every drawn node/edge is click-to-source to the exact line; no source, no element. (2) Fail-loud honesty — tree-sitter ERROR nodes render as visibly-marked 'unparsed' regions, never omitted, never fabricated. (3) Deterministic/reproducible — no AI at run time, no per-run uncertainty, same code=same picture; no spinners implying nondeterminism. (4) Zero-friction start — starter views run on install with no account, no config, no tokens. (5) Molding happens in the engineer's own AI assistant OUTSIDE the toolkit UI; the toolkit surface is the extension-contract/primitives read as code + docs.
- (assumption) DESIGN.md assumptions pending confirmation: (a) provenance accent = --vscode-charts-green ('grounded/verified'); (b) unparsed = editorWarning hue deliberately NOT error-red, so honest holes read 'known unknown' not 'broken'; (c) base controls align to @vscode/webview-ui-toolkit or equivalent theme-token styling; (d) near-square radii (2/3px) to match VS Code chrome.
- (assumption) EXPERIENCE.md assumptions pending confirmation: (a) 'Provenance rail' — slim collapsible right-side rail carrying source anchors for the current selection, collapsed by default; (b) 'View gallery' — a picker surface listing starter views and in-repo molded tools together; (c) starter view set named illustratively as dependency map / call-flow / module-reach tracer, pending PRD OQ-3 which deliberately leaves count+shape open.
- (event) Both spines drafted (fast path) from PRD+PRFAQ+distillate. DESIGN.md: theme-inherited VS Code identity, brand layer = provenance/trace/unparsed semantics only. EXPERIENCE.md: 7 IA surfaces, 2 invented sections (Provenance & Trust Model; Molding Handoff), 3 named-protagonist flows (Nomsa).
- (decision) CONFIRMED: full VS Code theme inheritance. No brand palette, no identity accent. Only provenance/trace-active/unparsed carry meaning-color, all theme-derived. DESIGN.md as drafted stands.
- (change) Provenance surfacing = BOTH (was: rail only). Hover/selection popover attached to the element is the quick, always-there anchor; the provenance rail becomes OPT-IN (toggled) for auditing many elements at once. Rail no longer auto-opens on select.
- (override) Starter view set CLOSED at three committed v1 surfaces, each behaviourally specified: (1) Dependency map, (2) Call-flow, (3) Module-reach tracer. This RESOLVES PRD OQ-3 (which deliberately left count+shape open) and expands v1 beyond FR-5's literal 'at least one working view'. Flag upstream to PRD as spine->PRD feedback.
- (assumption) OPEN, non-blocking (triaged, carried into build): (1) View gallery + gallery-item surface — assumed as the picker listing starter views and in-repo molded tools together; shape unconfirmed. (2) provenance accent = charts-green — cosmetic, swappable. (3) Base controls assumed aligned to @vscode/webview-ui-toolkit or equivalent theme-token styling — an architecture-phase call, not a UX one.
- (change) Applied both confirmed changes to spines. DESIGN.md: added anchor-popover + provenance-rail components, rail demoted to opt-in/never-auto-open. EXPERIENCE.md: dual provenance grain (popover=spot-check, rail=full audit) threaded through IA/components/primitives/a11y/responsive/trust-model/flows; added 'Starter View Set' section committing dependency map + call-flow + module-reach tracer with per-view behavior and the COBOL-Java v1 boundary.
- (change) NEW REQUIREMENT (user, pre-finalize): multi-perspective views. Same scope, switchable projections — e.g. module dependency map <-> sequence-diagram perspective; drill in/out of components (semantic zoom across grains); at function/method grain a state-machine or flowchart perspective (user asked facilitator to determine which works in most scenarios) plus a code view of the function/method. Reframes 'one view = one question' into 'one scope, multiple perspectives'. Spine edits pending decisions.
- (decision) Perspectives model CONFIRMED, committed FULLY to v1: one scope x switchable perspectives across three grains. System grain (node=program/module): dependency map, sequence diagram, module-reach tracer. Component grain (node=section/paragraph/function): call-flow. Unit grain (inside one paragraph/function): flowchart (control-flow) + code view. Drill = Enter/double-click down a grain, Esc/breadcrumb up; F12 = jump to source at every grain. Sequence diagram = static call order as written in source (guards shown on conditional calls), never a runtime trace.
- (decision) Unit grain: FLOWCHART ONLY in v1. State-machine perspective NOT bundled — inferring states from arbitrary code would be fabrication; it becomes the first-listed moldable-tool candidate (render only where a state construct is explicit, e.g. EVALUATE/switch on a state variable). Code view = NATIVE EDITOR SPLIT beside the canvas, selection-synced; the toolkit never re-renders source in a webview.
- (override) UPSTREAM FLAG (PRD feedback): committing perspectives + semantic zoom + sequence diagram + unit flowchart fully to v1 expands scope well beyond PRD FR-5's 'at least one working starter view' and further supersedes OQ-3's deliberate openness. PRD should be updated (bmad-prd update intent) and the reliability-gate sequencing (OQ-1) re-checked against the larger primitive surface.
- (event) GT research (book.gtoolkit.com Inspector chapter, humane-assessment GTInspector deconstruction, HN thread 43606027): GT inspector = Miller-columns pager (each drill spawns a live pane right, trail preserved spatially, back = walk the pane tunnel); views are contextual per object via <gtView> pragma, priority-ordered tabs; peek-before-spawn triangle affordance. Pain evidence: newcomers disoriented — panes shift, 'looks completely different without indication of what changed', symbols incomprehensible.
- (decision) GT lessons applied, three adoptions + one rejection: (1) ADOPT trail-preservation but TEMPORAL not spatial — Alt+Left/Right (VS Code Go Back/Forward) restores exact prior step (scope+perspective+selection); Esc stays structural retreat. Single canvas always. (2) ADOPT gtView extensibility — molded tools can register as perspectives (declare grain + node-kind applicability), appearing in the switcher contextually; requirement fed to extension-contract design (PRD OQ-2); state-machine = canonical example. (3) ADOPT peek-before-spawn — anchor popover gains one-line drill preview. (4) REJECT Miller-columns pane cascade — logged as explicit anti-pattern with HN evidence. Also: perspective switch states continuity in header ('Sequence — same 12 modules').
- (event) Reviewer Gate ran 3 lenses (rubric, accessibility, adversarial) + key-screens renderer. Consolidated: 5 critical, 9 high, 22 medium, 14 low. Verdicts: Flow coverage STRONG, Shape fit STRONG, five categories adequate, visual-reference pending. Convergent theme across lenses: the drawn picture is more resolved than extraction can prove, exactly where COBOL is hard (dynamic CALL targets, unparsed holes inside rendered structures, unexpanded copybooks). Second theme: a11y floor names outcomes with no mechanism. Reports: validation-report.html/.md + review-rubric.md, review-a11y.md, review-adversarial.md. Mocks rendered to .working/key-dependency-map.html and key-unit-flowchart.html.
- (override) USER CORRECTION on accessibility: 'i didn't ask for screen reader level of accessibility'. The a11y reviewer was dispatched at WCAG 2.2 AA incl. SR parity, and the DRAFT spine had already claimed SR announcements unprompted. RESOLUTION: right-size the Accessibility Floor to what v1 actually commits — keyboard operability, theme/contrast inheritance, colour-never-sole-cue, reduced motion — and state plainly that AT/screen-reader support is NOT a v1 commitment (justified by PRD s2.2: sole maintainer is the only v1 user; other engineers explicitly not a v1 adoption/support target). Do NOT silently keep an aspirational floor: an unmet claim is the overclaim this product refuses. REJECTED from a11y review: C1 ARIA model + Structure view, C2 text equivalents for marks, M3 Esc announcements, M5 live regions, M8 required-accessible-name primitive, L1 glyph naming.
- (decision) KEPT from a11y review (serve the sighted keyboard user / are honesty fixes, not SR machinery): H1 canvas as single tab stop w/ roving focus (Tab-trap fix), H3 contrast floor + charts-* fallback chains + min tick size, H4 keybinding portability (webviews get raw key events; Alt+Left is not Go Back on macOS; primitives must also be remappable VS Code commands), M1 trace highlight persists (DESIGN said fades - EXPERIENCE correct), M2 popover must stay hoverable/dismissible, M4 high-contrast claim softened to honest, M6 Code-split must not steal focus, M7 reduced-motion + legibility floor.
- (decision) Phasing CONFIRMED: Phase A/B named inside the spine; perspective MODEL stays fully committed so Phase B needs no redesign. Phase A (reliability-gate-sufficient): dependency map + unit flowchart, anchor popover, jump-to-source, unparsed rendering, gallery-as-command-list. Phase B (post-gate): sequence, module-reach, call-flow, trail history, provenance rail, Code-perspective sync, molded-perspective registration. Copybooks CONFIRMED: commit to EXPANSION in v1 (resolve COPY incl. REPLACING during extraction) - most faithful to real COBOL; provenance must carry both the COPY site and the copybook definition line so click-through never surprises.
- (event) spines finalized (status: final, updated 2026-07-19). Resolved from the gate: all 5 criticals (PRD supersession declared in-artifact; Flow 3 Java overclaim rewritten to an honest COBOL-only question; dynamic-CALL 'unresolved target' added as a third first-class honesty state; a11y C1/C2 REJECTED per user scoping and the floor right-sized instead of left aspirational) + 8 of 9 highs. Mocks promoted to mockups/. NOT run: bmad-editorial-review-structure / -prose (doc_standards) — spines already went through 3 review lenses; available on request. OUTSTANDING UPSTREAM: PRD needs updating for OQ-3 closure, FR-5 expansion, and Phase A/B sequencing vs OQ-1.
- (override) USER CORRECTION: 'the views are going to be agnostic of the language' — the spine had been written in COBOL nouns (grain table said 'section/paragraph (COBOL)', edges were CALL/COPY/SELECT-FD, Boundary said 'all perspectives are COBOL-facing'). Conceded: the model IS agnostic and v1 already carries two languages (PRD FR-2 TypeScript), so a COBOL-keyed spine forced a rewrite per language against the PRFAQ's 'contribute a language' flywheel. REFACTOR APPLIED: all normative text hoisted to generic terms — grains = compilation unit / named callable / control flow within a callable; edges = static reference, invocation, unstructured jump; the three honesty states restated as GENERAL CATEGORIES (dynamic dispatch / parse-failure region / included-generated source) with COBOL as one instance. New 'Language Profiles' section is the ONLY place a language name belongs: a table mapping generic concepts to COBOL + TypeScript constructs. Adding a language = one column + a grammar, no spine edit. Key Flows deliberately keep COBOL (a named-protagonist journey needs a concrete codebase; illustrative, not normative). Token edge-goto renamed edge-jump.
- (change) GAP FOUND + CLOSED (user question, 2026-07-19): inbound navigation was missing entirely — 'called by / callers / inbound / dependents / reverse / fan-in' appeared NOWHERE in either spine or the PRD. Every specified perspective pointed outward (reach tracer 'reads, writes, invokes'; sequence 'from a chosen entry point'; dependency-map edges 'one unit naming another'). Notable: the PRFAQ's original molding example DID include an inbound query ('show me where the Java invokes it') — it was cut with the Java non-goal and the inbound direction was never separately preserved. Also a framing mismatch: the PRD called the reach tracer 'the blast-radius perspective' while specifying only the outbound direction, but blast radius for changing X is predominantly who depends on X. DECISION: make the reach tracer BIDIRECTIONAL (direction toggle: outbound / inbound / both) rather than adding a seventh perspective — reuses its depth-truncation discipline and honest-state vocabulary.
- (constraint) INBOUND COMPLETENESS is a strictly harder honesty problem than outbound, and is now a hard rule. Outbound: 'X invokes something unresolvable' — the unknown is visible and the existing unresolved-target state covers it. Inbound: 'nothing reaches X' can be FALSE, because any dynamic-dispatch site in the estate might resolve to X at runtime. So an inbound result is NEVER a bare count or a bare empty state: it always states the unresolvable-dispatch population that could reach the target ('4 static callers, plus 12 dynamic call sites whose targets are not statically resolvable — any could reach this'). An empty inbound result must never read as 'unused' or 'safe to delete' — that false negative is precisely the confidently-wrong, business-breaking failure the product exists to refuse. Banned: any disposal/dead-code affordance derived from inbound emptiness.
- (decision) DATA ARTIFACTS BECOME FIRST-CLASS NODES (user, 2026-07-19). Driver: the real questions are 'what depends on this DB table / file / program' (inbound) and 'once this component is invoked, what else does it touch' (outbound). Direction alone did not answer the first — the thing the user wants to SELECT was not a node. System grain admitted only compilation units; files appeared once as an edge-label example; DB tables and EXEC SQL appeared NOWHERE in either document (verified by grep). Also fixed an incoherence introduced with the bidirectional tracer: it claimed first-class read/write edge kinds while no node type existed on the far end of a write. RESOLUTION: system grain now admits programs, copybooks, FILES and TABLES. Data artifacts are LEAF nodes — no component grain, so drilling is unavailable (availability rule covers it) and the natural action on one is inbound reach.
- (change) RECONCILED WITH ARCHITECTURE SPINE (2026-07-19). Four drifts closed in both directions: (1) DETERMINISM — PRD s4's 'Not pixel-stable view layout at v1' non-goal STRUCK, architecture took the fork it was handed (AD-7/AD-14); EXPERIENCE.md's 'Determinism copy names structure, not pixels' rule inverted to 'may claim the picture, per layout selection'; voice-table row updated to 'same code, same picture'. (2) GATE SLICE GREW — data artifacts (files AND tables) now inside it, in PRD s5.2, its Glossary entry, and the EXPERIENCE phasing table; the grammar discovery removed the cost that justified deferring tables. (3) FOURTH HONEST STATE — 'out-of-snapshot reference' (statically named, not extracted) added to PRD s3.1 + FR-1 consequences, EXPERIENCE's over-resolve section, and DESIGN.md as node-out-of-snapshot (dotted/muted, distinct from dashed-unparsed and dashed-unresolved). (4) FR-1 grammar line now reflects EXEC SQL/CICS being typed by the grammar rather than needing a second parser. Also cleared EXPERIENCE's stale 'supersede PRD OQ-3 pending the PRD update' header note.
